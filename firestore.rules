rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /waitlist/{doc} {
      allow read, write: if true;
    }
    match /bookings/{doc} {
      allow read, write: if true;
    }
  }
}
